<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_main_gradient">

    <!-- AppBar with Gaming Toolbar -->
    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/appBarLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#1036F449"
        app:elevation="0dp">

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="#CC0A0A1A"
            app:navigationIcon="@drawable/baseline_arrow_back_24"
            app:title="🔐 Reset Password"
            app:titleTextColor="#00E5FF"
            />

    </com.google.android.material.appbar.AppBarLayout>

    <!-- Scrollable Content -->
    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior"
        android:fillViewport="true">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="20dp">

            <!-- ── TOP ICON + TITLE SECTION ── -->
            <ImageView
                android:id="@+id/ivLockIcon"
                android:layout_width="72dp"
                android:layout_height="72dp"
                android:src="@drawable/baseline_arrow_back_24"
                android:layout_marginTop="16dp"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:tint="#00E5FF" />

            <TextView
                android:id="@+id/tvMainTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="PASSWORD RESET"
                android:textColor="#00E5FF"
                android:textSize="22sp"
                android:textStyle="bold"
                android:letterSpacing="0.15"
                android:layout_marginTop="10dp"
                app:layout_constraintTop_toBottomOf="@id/ivLockIcon"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent" />

            <TextView
                android:id="@+id/tvSubTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Choose your preferred reset method"
                android:textColor="#80FFFFFF"
                android:textSize="13sp"
                android:layout_marginTop="4dp"
                app:layout_constraintTop_toBottomOf="@id/tvMainTitle"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent" />

            <!-- ── NEON DIVIDER ── -->
            <View
                android:id="@+id/divider1"
                android:layout_width="0dp"
                android:layout_height="1.5dp"
                android:background="#2200E5FF"
                android:layout_marginTop="16dp"
                android:layout_marginHorizontal="8dp"
                app:layout_constraintTop_toBottomOf="@id/tvSubTitle"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent" />

            <!-- ── MAIN CARD ── -->
            <com.google.android.material.card.MaterialCardView
                android:id="@+id/cardReset"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                app:cardBackgroundColor="#0D0D1F"
                app:cardCornerRadius="16dp"
                app:strokeColor="#2200E5FF"
                app:strokeWidth="1.5dp"
                app:cardElevation="12dp"
                app:layout_constraintTop_toBottomOf="@id/divider1"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent">

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    android:padding="20dp">

                    <!-- Email Label -->
                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="📧  EMAIL ADDRESS"
                        android:textColor="#00E5FF"
                        android:textSize="11sp"
                        android:letterSpacing="0.12"
                        android:layout_marginBottom="6dp"/>

                    <!-- Email Input — Gaming Style -->
                    <com.google.android.material.textfield.TextInputLayout
                        android:id="@+id/tilEmail"
                        style="@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        app:boxStrokeColor="#00E5FF"
                        app:boxStrokeWidth="1.5dp"
                        app:boxBackgroundColor="#0A0A1A"
                        app:hintTextColor="#80FFFFFF"
                        app:boxCornerRadiusTopStart="10dp"
                        app:boxCornerRadiusTopEnd="10dp"
                        app:boxCornerRadiusBottomStart="10dp"
                        app:boxCornerRadiusBottomEnd="10dp"
                        app:startIconDrawable="@drawable/baseline_arrow_back_24"
                        app:startIconTint="#00E5FF">

                        <com.google.android.material.textfield.TextInputEditText
                            android:id="@+id/etEmail"
                            android:layout_width="match_parent"
                            android:layout_height="wrap_content"
                            android:hint="Enter your email"
                            android:inputType="textEmailAddress"
                            android:textColor="#FFFFFF"
                            android:textColorHint="#60FFFFFF"
                            android:paddingVertical="14dp" />

                    </com.google.android.material.textfield.TextInputLayout>

                    <!-- Method Label -->
                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="⚙  SELECT RESET METHOD"
                        android:textColor="#FFD700"
                        android:textSize="11sp"
                        android:letterSpacing="0.12"
                        android:layout_marginTop="20dp"
                        android:layout_marginBottom="10dp"/>

                    <!-- ── OTP RESET BUTTON ── Gaming Primary -->
                    <com.google.android.material.button.MaterialButton
                        android:id="@+id/btnOtpReset"
                        android:layout_width="match_parent"
                        android:layout_height="48dp"
                        android:text="⚡  RESET VIA OTP"
                        android:textColor="#0A0A1A"
                        android:textSize="13sp"
                        android:textStyle="bold"
                        android:letterSpacing="0.08"
                        app:backgroundTint="#00E5FF"
                        app:cornerRadius="10dp"
                        app:elevation="6dp"
                        android:layout_marginBottom="10dp"
                        app:icon="@null"
                        app:rippleColor="#40FFFFFF"/>

                    <!-- OTP Hint Badge -->
                    <com.google.android.material.chip.Chip
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="✅ Fast &amp; Instant — Recommended"
                        android:textColor="#00E5FF"
                        android:textSize="11sp"
                        app:chipBackgroundColor="#1400E5FF"
                        app:chipStrokeColor="#2200E5FF"
                        app:chipStrokeWidth="1dp"
                        android:layout_marginBottom="14dp"
                        android:layout_gravity="center_horizontal"
                        android:clickable="false"
                        android:focusable="false"/>

                    <!-- Divider between buttons -->
                    <View
                        android:layout_width="match_parent"
                        android:layout_height="1dp"
                        android:background="#1AFFFFFF"
                        android:layout_marginBottom="14dp"/>

                    <!-- ── EMAIL RESET BUTTON ── Gaming Secondary -->
                    <com.google.android.material.button.MaterialButton
                        android:id="@+id/btnResetPassword"
                        android:layout_width="match_parent"
                        android:layout_height="44dp"
                        android:text="📧  RESET VIA EMAIL LINK"
                        android:textColor="#FFFFFF"
                        android:textSize="12sp"
                        android:letterSpacing="0.06"
                        app:backgroundTint="#1A1A2E"
                        app:cornerRadius="10dp"
                        app:strokeColor="#40FFFFFF"
                        app:strokeWidth="1dp"
                        app:rippleColor="#2200E5FF"/>

                </LinearLayout>

            </com.google.android.material.card.MaterialCardView>

            <!-- ── STATUS CARD (Loading / Countdown / Messages) ── -->
            <com.google.android.material.card.MaterialCardView
                android:id="@+id/cardStatus"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginTop="14dp"
                android:visibility="gone"
                app:cardBackgroundColor="#0D0D1F"
                app:cardCornerRadius="12dp"
                app:strokeColor="#22FFD700"
                app:strokeWidth="1dp"
                app:layout_constraintTop_toBottomOf="@id/cardReset"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent">

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    android:gravity="center"
                    android:padding="16dp">

                    <!-- Progress Bar (indeterminate) -->
                    <com.google.android.material.progressindicator.LinearProgressIndicator
                        android:id="@+id/progressBar"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:indeterminate="true"
                        android:visibility="gone"
                        app:indicatorColor="#00E5FF"
                        app:trackColor="#1A00E5FF"
                        app:trackCornerRadius="4dp" />

                    <!-- Status Text -->
                    <TextView
                        android:id="@+id/tvResetInfo"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="Processing..."
                        android:textColor="#80FFFFFF"
                        android:textSize="13sp"
                        android:gravity="center"
                        android:layout_marginTop="8dp"/>

                    <!-- Countdown Timer -->
                    <TextView
                        android:id="@+id/tvCountdown"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:textColor="#FFD700"
                        android:textSize="14sp"
                        android:textStyle="bold"
                        android:gravity="center"
                        android:layout_marginTop="4dp"
                        android:visibility="gone"/>

                </LinearLayout>

            </com.google.android.material.card.MaterialCardView>

            <!-- ── SUPPORT TEXT ── -->
            <TextView
                android:id="@+id/tvSupport"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Need help?  edmfiresupport@gmail.com"
                android:textColor="#4000E5FF"
                android:textSize="12sp"
                android:layout_marginTop="16dp"
                app:layout_constraintTop_toBottomOf="@id/cardStatus"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent" />

            <!-- ── BACK TO LOGIN BUTTON ── Ghost Style -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnBackToLogin"
                android:layout_width="0dp"
                android:layout_height="44dp"
                android:text="← BACK TO LOGIN"
                android:textColor="#60FFFFFF"
                android:textSize="12sp"
                android:letterSpacing="0.06"
                android:layout_marginTop="10dp"
                android:layout_marginBottom="20dp"
                app:backgroundTint="#00000000"
                app:cornerRadius="10dp"
                app:strokeColor="#20FFFFFF"
                app:strokeWidth="1dp"
                app:rippleColor="#1AFFFFFF"
                app:layout_constraintTop_toBottomOf="@id/tvSupport"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent" />

        </androidx.constraintlayout.widget.ConstraintLayout>

    </androidx.core.widget.NestedScrollView>

</androidx.coordinatorlayout.widget.CoordinatorLayout>